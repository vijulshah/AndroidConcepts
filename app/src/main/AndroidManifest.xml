<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="royal.com.androidconcepts">

    <application
        android:allowBackup="true"
        android:icon="@drawable/android"
        android:label="@string/app_name"
        android:roundIcon="@drawable/android"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrackLocation"></activity>

        <activity android:name=".LocationActivity" />

        <activity
            android:name=".NotificationView"
            android:parentActivityName=".NotificationService">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".NotificationService" />
        </activity>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".FragmentLifeCycle" />
        <activity android:name=".ActivityLifeCycle" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomListView" />
        <activity android:name=".RadioButtons" />
        <activity android:name=".CheckBoxesClass" />
        <activity android:name=".SimpleToast" />
        <activity android:name=".CustomToast" />
        <activity android:name=".SimpleDialogBox" />
        <activity android:name=".CustomDialogBox" />
        <activity android:name=".SimpleSeekBar" />
        <activity android:name=".CustomSeekBar" />
        <activity android:name=".SimpleRatingBar" />
        <activity android:name=".CustomRatingBar" />
        <activity android:name=".SimpleListView" />
        <activity android:name=".ChangeImage" />
        <activity android:name=".TextView_EditText" />
        <activity android:name=".SimpleSpinner" />
        <activity android:name=".CustomSpinner" />
        <activity android:name=".GridViewClass" />
        <activity android:name=".CardViewClass" />
        <activity android:name=".WebViewClass" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TimePickerClass" />
        <activity
            android:name=".AllMenus"
            android:label="Options Menu"
            android:theme="@style/AllMenusActivity" />
        <activity android:name=".SendMessage" />
        <activity android:name=".StaggeredGridViewClass" />
        <activity
            android:name=".MainPage"
            android:label="@string/title_activity_main_page"
            android:theme="@style/AppTheme" />
        <activity android:name=".SharedPref" />
        <activity android:name=".ActivityResultClass" />
        <activity android:name=".Sharedpref_result" />
        <activity android:name=".MusicService" />
        <activity android:name=".BroadcastService" />
        <activity android:name=".NotificationService" />
        <activity android:name=".RecyclerViewClass" />
        <activity android:name=".TabFrag" />
        <activity android:name=".DB_MainPage" />
        <activity android:name=".AddDetails" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <receiver android:name=".DoBroaadcast" />

        <service android:name=".MyMusicPlayer" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>
    <uses-feature android:name="android.hardware.camera" />

    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

</manifest>